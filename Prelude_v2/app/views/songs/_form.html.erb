<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :artist %><br>
    <%= f.text_field :artist %>
  </div>
  <!-- Add two fields for mp3 and chart -->
  <div class="field">
    <%= f.label :chart %>
    <h6>
      <%= f.file_field :chart %>
    </h6>
  </div>
  <div class="field">
    <%= f.label :mp3 %>
    <h6>
      <%= f.file_field :mp3 %>
    </h6>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
